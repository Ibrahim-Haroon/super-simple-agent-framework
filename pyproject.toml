[project]
name = "super-simple-agent-framework"
version = "0.1.0"
description = "Framework for building simple agents with Python. Perfect for hacking and learning."
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
]
